const numbers = [1, 2, 3];

const join = numbers.join(',');

console.log('Joined:', join);

const postTitle = 'How to join arrays in javascript';
const parts = postTitle.split(' ');

console.log('Parts:', parts);

slug = parts.join('-').toLowerCase();

console.log('Slug URL:', slug);